<!DOCTYPE html>
<html lang="en">
<head>
  <% include partials/head %>
</head>
<body class="bg-light">
  <% include partials/menu %>
  <div class="container">
    <div class="row">
      <div class="col-md-10">
        <h4 class="mb-3">Cadastro de Orientações</h4>
        <form method="post" action="/addorientacao" class="needs-validation" novalidate>
          <div class="row">
            <div class="col-md mb-3">
              <label for="name">Nome do Orientando</label>
              <input type="text" class="form-control is-invalid" name="aluno" id="name" aria-describedby="inputGroupPrepend3" required>
              <div class="invalid-feedback">
                Por favor, digite o nome do Orientando.
              </div>
            </div>
          </div>
          <div class="mb-3">
            <label for="titulo">Título do Projeto Homologado</label>
            <input type="text" class="form-control is-invalid" name="titulo" id="titulo" aria-describedby="inputGroupPrepend3" placeholder="" required>
            <div class="invalid-feedback">
              Por favor digite o título do projeto.
            </div>
          </div>
          <div class="mb-3">
            <label for="professor">Professor</label>
            <select class="custom-select d-block w-100 is-invalid" name="professor" id="professor" aria-describedby="inputGroupPrepend3" required>
              <option value="Escolha">Escolha...</option>
              <% professors.forEach(function(professor) { %>
                  <option value="<%= professor._id %>"><%= professor.nome %></option>
              <% }); %>
            </select>
            <div class="invalid-feedback">
              Por favor, escolha um professor
            </div>
            <hr class="mb-4">
            <button class="btn btn-primary btn-lg btn-block" type="submit">Salvar</button>
          </form>
        </div>
      </div>
    </div>
    <div class="col-md-10">
      <table class="table">
        <thead>
          <tr>
            <th scope="col">Aluno</th>
            <th scope="col">Título</th>
            <th scope="col">Professor</th>
          </tr>
        </thead>
        <tbody>
          <% orientacao.forEach(function(orienta) { %>
            <tr>
              <td><%= orienta.aluno %></td>
              <td><%= orienta.titulo %></td>
              <td><%= orienta.professor._id %></td>
              <!-- <% professors.forEach(function(professor) { %>
                <% if(professor._id == orienta.professor._id){ %>
                  <td><%= professor.nome %></td>
                <%}%>
              <% }); %> -->

            </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
    </div>

    <% include partials/script %>

  </body>
  </html>
